server.servlet.context-path=/
server.port=9000

spring.datasource.url=jdbc:mysql://donpepe.cgfauwbxunzx.us-east-1.rds.amazonaws.com:3306/donpepe?serverTimezone=America/Lima&useSSL=false&publicKeyRetrieval=true
spring.datasource.username=admin
spring.datasource.password=tienditadedonpepe
spring.jpa.properties.hibernate.format_sql=true

spring.session.store-type=jdbc
spring.session.jdbc.initialize-schema=always

## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=300MB
# Max file size.
spring.servlet.multipart.max-file-size=300MB
# Max Request Size
spring.servlet.multipart.max-request-size=300MB
